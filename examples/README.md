# Examples

- **simple.py** — tap mic, speak, see result. Minimal code.
- **continuous.py** — auto-restarts recognition on silence timeout for pseudo-continuous dictation.
- **locale_picker.py** — lists available languages, pick one, then listen in that language.

All examples require `flet-stt` installed. Run on Android (or iOS) — desktop has no OS speech recognizer.
